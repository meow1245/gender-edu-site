.avatar-btn {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid white;
    cursor: pointer;
}

#avatarMenu {
    position: absolute;
    top: 70px;
    right: 20px;
    background: var(--card);
    width: 180px;
    padding: 10px 0;
    border-radius: var(--radius);
    box-shadow: 0 4px 12px #0002;
    display: none;
}

#avatarMenu a {
    display: block;
    padding: 12px 18px;
}

#avatarMenu a:hover {
    background: #f2f2f2;
}
